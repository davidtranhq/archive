#ifndef QUICKSORT
#define QUICKSORT

void quicksort(int a[], int left, int right);
/*
    quicksort: sorts array a using a quick sort algorithm. requires the
    left and right indexes of the (sub)array to be sorted.
*/

#endif